<%= form_for(@calender) do |f| %>
    <div class="field">
      <%= f.text_area :title, placeholder: 'Compose new Calender' %>
    </div>
    <%= f.submit 'Post', class: 'btn btn-primary' %>
<% end %>
